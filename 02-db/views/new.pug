extends layout

block content
  .row.justify-content-center
    .col-md-6
      form(action='/new', method='POST', class='card p-4 shadow-sm')
        .mb-3
          input.form-control(type='text', name='title', placeholder='Введите задачу', required)
        button.btn.btn-primary(type='submit') Добавить
